# Photo Doctor 0.5.14 — session log

## Причина изменения
Пользователь прислал скриншоты ALMAZ и ИИ: данные визуально обрезались, секции тратили высоту на рамки и внутренние поля QGroupBox, пустой ALMAZ-log занимал большую область, runtime-кнопки не помещались по ширине.

## Что изменено
- `_build_almaz_tab`: QScrollArea + плоские секции с заголовками/разделителями; без QGroupBox.
- ALMAZ activity log скрыт по умолчанию, добавлен toggle.
- Runtime controls ALMAZ разбиты на две строки.
- `_build_ai_tab`: QScrollArea + плоские секции; без QGroupBox в простом режиме.
- Сохранены расширенные AI-вкладки и все существующие функции.
- Добавлены/обновлены source-regression тесты на плоскую компоновку.

## Не менялось
- Анализ изображения, correction pipeline, Validator, ALMAZ inference, модели, алгоритмический ключ и кэш.

## Следующая точка
После проверки пользователем реального окна оценить, достаточно ли двухстрочного runtime-блока или длинные действия лучше перенести в меню «Ещё».
